@model Brand
@{
    ViewData["Title"] = "Update";
}

<div class="row">
    <div class="col-lg-12">
        <h1>Update Product</h1>
    </div>
</div>
<div class="productupdate">
    @await Html.PartialAsync("_BrandUpdateImagePartial", Model)
</div>
<div class="row">
    <div class="col">
        <a class="btn btn-primary" asp-action="Index">Go To List</a>
    </div>
</div>

@section Script{
    <partial name="_ValidationFormScriptPartial">
   @*     <script>
         $(document).on("click", "#deleteImage", function (e) {
        e.preventDefault();

        let url = $(this).attr("href");

        fetch(url).then(response => { return response.text() }).then(data => { $(".productupdate").html(data) });
         })
        </script>*@
}
